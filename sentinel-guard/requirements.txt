# Core engine — zero external dependencies (stdlib only)
# The engine (scanner, heuristics, quarantine, etc.) works with pure Python stdlib

# Web GUI dependencies
flask>=3.0.0

# Optional: enhanced monitoring (Linux only)
# pyinotify==0.9.6; sys.platform == "linux"

# Optional: YARA rule support
# yara-python>=4.3.0

# Optional: Rich terminal output
# rich>=13.7.0
